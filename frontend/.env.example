# Optional production overrides. Leave unset for local development and Quick Tunnel testing.
# VITE_API_BASE=https://api.example.com
# VITE_WS_BASE=wss://api.example.com

# When running Vite locally, /api and /ws are proxied to FastAPI on port 8000.
